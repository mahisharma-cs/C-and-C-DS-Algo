#include<stdio.h>
int main(){
	int a[10], b, c, d=0, Avg;
	printf("Enter number of subjects: ");
	scanf("%d", &b);
	for(b=0 ; b<c ; ++b)
	{
	printf("Enter number %d :", b+1);
	scanf("%d", &a[b]);
	d += a[i];
	}
	Avg = d/b;
	printf("Average = %d", Avg);
}